import express from 'express';
const router = express.Router();

// Placeholder route - TODO: Implement collaboration endpoints
router.get('/', (req, res) => {
  res.json({ success: true, data: [], message: 'Collaboration endpoints - TODO' });
});

export default router;